name: Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check URL Health
        uses: jtalk/url-health-check-action@v4
        with:
          url: https://full-stack-open-pokedex-z02j.onrender.com/health